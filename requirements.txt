# Núcleo
anthropic>=0.49.0
langgraph>=0.2.30
pydantic>=2.6
pydantic-settings>=2.2
requests>=2.31
python-dotenv>=1.0

# Opcional — sólo si IMAGE_PROVIDER=openai
openai>=1.40

# Desarrollo / tests (instalá con: pip install -r requirements.txt)
pytest>=8.0
